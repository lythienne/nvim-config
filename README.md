My barebones Neovim setup + configuration

Uses Lazy as plugin manager. Includes:
 - Basic code capabilities: autopairs, indent-lines
 - Syntax highlighting: treesitter, Mason for LSP management
 - File navigation: neo-tree, telescope
 - Other: which-key, mellifluous colorscheme

also has some macros in remap.lua
